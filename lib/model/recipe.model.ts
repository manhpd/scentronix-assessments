export interface Recipe {
    id: string
    name: string
    slug: string
    description: string
    image: string
    ingredients: string[]
    instructions: string
    createdAt: string
    updatedAt: string
}